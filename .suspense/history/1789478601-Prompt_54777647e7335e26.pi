from /scope/application import ApplicationScope

anchor Prompt_54777647e7335e26:
    userPrompt:
        @{ApplicationScope} add dark mode to the MainWindow spec.  And for the LSP aspect of the spec, capture what the code currently integrates with.  Also the autocomplete popup needs to be location-aware because right now it falls off the edge of the screen.  Also the text input doesn't resize.
