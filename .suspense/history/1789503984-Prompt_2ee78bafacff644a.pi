from /scope/prompt-mode/chat-input import ChatInputScope

anchor Prompt_2ee78bafacff644a:
    userPrompt:
        The "+" in the @{ChatInputScope} tabs should be kind of "floating" between the code and spec tabs with its own outline.  That means the real tabs will need some extra padding right and left
        
        Also shift-tab should reverse cycle

    systemPrompt:
        We're working on the code located in ./src. Don't edit the spec located in ./spec.
