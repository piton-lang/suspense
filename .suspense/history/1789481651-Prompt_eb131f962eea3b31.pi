from /scope/prompt-mode/chat-input import ChatInputScope

anchor Prompt_eb131f962eea3b31:
    userPrompt:
        @{ChatInputScope} autoexpanding doesn't work very well.  I type a line, then I hit enter, and it scrolls beyond the first line and I can't see it anymore.  There's also a huge around of blank space (like a full line) below it.  To be clear, this is only when I explicitly create a new line.
