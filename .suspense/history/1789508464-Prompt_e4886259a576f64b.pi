from /scope/prompt-mode/message-list import MessageList

anchor Prompt_e4886259a576f64b:
    userPrompt:
        Can you make sure the @{MessageList} json stream "preview" is syntax highlighted?  And if there's something like "bash" or "edit" or whatever (you figure out the list which I think we've already referenced in the code) don't show it there until it's properly meaningful and you replace the JSON with something else.

    systemPrompt:
        We're working on the code located in ./src. Don't edit the spec located in ./spec.
