from /scope/prompt-mode/message-list import MessageList

anchor Prompt_80aad308dd4ddecd:
    userPrompt:
        Let's do one more thing.  For the main "body" column in the table for the @{MessageList} instead of showing a loading skeleton, let's actually show a 3 row stream of the raw json until we have something meaningful to show.  And while we're at it, remove the "View Raw Json" thing entirely.

    systemPrompt:
        We're working on the code located in ./src. Don't edit the spec located in ./spec.
