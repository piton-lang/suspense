from /scope/prompt-mode/chat-input import ChatInputScope

anchor Prompt_69eaa3fdcfcd5a8a:
    userPrompt:
        Let's add an "Ask" tab to the @{ChatInputScope} that allows us to simply ask a question and is specifically informed (via a systemPrompt) to not edit either the src/ or spec/ as read from and injected by the piton.config.pi

    systemPrompt:
        We're working on the code located in ./src. Don't edit the spec located in ./spec.
