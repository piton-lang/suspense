from /scope/prompt-mode/chat-input import ChatInputScope

anchor Prompt_d14ad08357f2326b:
    userPrompt:
        The results for an "ask" response should slide "up" from the @{ChatInputScope} and should execute separately from any running code or spec task.  The progress and results should still be displayed in a task table, however while it's thinking the task table should be a single row above the tabs of the @{ChatInputScope} and should only expand once the ask task is completed

    systemPrompt:
        We're working on the code located in ./src. Don't edit the spec located in ./spec.
