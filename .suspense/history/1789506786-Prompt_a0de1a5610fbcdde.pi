
anchor Prompt_a0de1a5610fbcdde:
    userPrompt:
        There should be no focus border around the chain icon to merge code and spec.  And in fact, it should be 50% dimmed when deactivated.  And let's go even further.  When it's not active, it should no overlap the code and spec tabs, but when it is activated, the tabs should animate closer together to signify that they're locked, while the icon remains in the same place as it is now.

    systemPrompt:
        We're working on the spec located in ./spec. Don't edit the code located in ./src.
