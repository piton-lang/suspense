
anchor Prompt_2c407f8b72aa7f50:
    userPrompt:
        -
            - "So we're definiting hitting an issue where, as the codebase and spec grows, the build gets a hell of a lot slower.  It actually seems like the consistency of the build got a lot worse when I added more skills referencing scope.  Can you weight in on both of those ?"

    mode: ask

    systemPrompt:
        -
            - "We're only asking a question about the code located in ./src and the spec located in ./spec. Don't edit either of them."
