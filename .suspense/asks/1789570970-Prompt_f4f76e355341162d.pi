from /scope/prompt-mode/chat-input import ChatInputScope

anchor Prompt_f4f76e355341162d:
    userPrompt:
        Should tab key alone be the switcher for the @{ChatInputScope} mode tabs ?

    mode: ask

    systemPrompt:
        We're only asking a question about the code located in ./src and the spec located in ./spec. Don't edit either of them.
