# Cargo build output
/target

# suspense's per-machine state. The prompt history is kept; the harness
# report (the local harness's installed skills/agents) and the send queue are
# regenerated as the app runs.
/.suspense/harness.json
/.suspense/queue/
/.suspense/commit-notes.json
/.suspense/conversations.json
.suspense-draft.pi

# Claude Code personal settings
/.claude/settings.local.json

# Editors and OS
.idea/
.vscode/
*.swp
*~
.DS_Store
.claude
